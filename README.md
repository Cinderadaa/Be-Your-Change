# Be Your Change – Interactive Journey

โปรเจกต์เว็บเกมถาม–ตอบแบบ Interactive สำหรับโครงการ **Be Your Change (BYC)**  
ออกแบบมาให้น้อง ๆ KMUTT ได้สำรวจตัวเองแบบนุ่มนวล ผ่านบทสนทนาและ Archetype ที่สะท้อน “ด้านในใจ” ของตัวเอง

> “หายใจเข้าเบา ๆ แล้วลองดูภาพสะท้อนหัวใจของตัวเองไปด้วยกัน”

---

## 🔍 Concept & Experience

- ผู้เล่นกรอก **ชื่อเล่น** → เข้าสู่เส้นทางการเดินทางภายใน (Journey Within)
- ผ่านชุดคำถามสั้น ๆ ที่เล่าเป็น **เรื่องราว / ฉาก** (Storytelling Question Flow)
- แต่ละคำตอบจะสะสมไปสู่ **Archetype** ของผู้เล่น เช่น `Seed`, `Mirror`, `River`, ฯลฯ  
- หน้าสุดท้ายจะแสดงผลลัพธ์เป็น **การ์ดสรุป Archetype** พร้อมประโยคเตือนใจ และปุ่มสมัครเข้าร่วมโครงการ BYC

โทนภาพรวม: Glassmorphism / 3D Liquid / Soft Gradient ตาม CI ของ BYC

---

## 🧱 Tech Stack

- **Framework**: Next.js (App Router)
- **Language**: JavaScript
- **Styling**: Global CSS + custom glass UI
- **Font**: [Kanit](https://fonts.google.com/specimen/Kanit)
- **Other**:
  - Typewriter-style narrative component
  - API routes (`/api/evaluate`, `/api/question`, `/api/save`) สำหรับประมวลผลคำตอบ

---

## 📁 โครงสร้างโปรเจกต์คร่าว ๆ

> โฟลเดอร์/ไฟล์ที่สำคัญ

```bash
app/
  layout.js           # Shell หลัก + Glass Navbar
  page.js             # หน้า Intro / กรอกชื่อเล่น
  soft/page.js        # หน้า Soft Narrative ก่อนโชว์ผลลัพธ์
  card/page.js        # หน้าตัวอย่างการ์ด (IG card)
  question/[id]/      # หน้าแสดงคำถามแต่ละข้อ
  result/page.js      # หน้าแสดง Archetype ของผู้เล่น
  api/
    evaluate/route.js # ประมวลผลคะแนน/ประเภท Archetype
    question/route.js # ดึงข้อมูลคำถาม
    save/route.js     # บันทึกผลลัพธ์ (เช่น ส่งไป Supabase)

components/
  HoloCard.js         # กล่องการ์ด Glass หลัก
  ChoiceButton.js     # ปุ่มตัวเลือกคำตอบแบบ Pill
  BackButton.js       # ปุ่มย้อนกลับคำถาม
  Typewriter.js       # ตัวพิมพ์เล่าเรื่องทีละบรรทัด
  FogOverlay.js       # ชั้นแสง/หมอกด้านหลังจาก 3D icons

lib/
  questions.js        # ข้อมูลคำถาม
  archetypes.js       # ข้อมูล Archetype, meaning, affirmations
  supabase.js         # (ถ้าใช้) config สำหรับ Supabase

public/
  bg.png                              # พื้นหลัง gradient
  seed.png, mirror.png, river.png   # 3D icons แต่ละ Archetype
  lighthouse.png, bridge.png, ...   # 3D icons ใช้ในฉาก
  byc_logo.png                       # โลโก้ BYC
  favicon.png, apple-touch-icon.png # เว็บไอคอน / iOS icon
```

## 🚀 การรันโปรเจกต์ (Getting Started)

ก่อนเริ่ม ให้แน่ใจว่าติดตั้ง **Node.js (แนะนำ LTS)** และ **npm** ไว้แล้ว

### 1. ติดตั้ง Dependencies

หลังจาก clone โปรเจกต์ลงเครื่อง ให้รันครั้งแรกว่า:

```bash
npm install
```

คำสั่งนี้จะติดตั้งแพ็กเกจทั้งหมดตามที่ระบุใน `package.json`

---

### 2. รันในโหมดพัฒนา (Development)

```bash
npm run dev
```

จากนั้นเปิดเบราว์เซอร์ไปที่:

```text
http://localhost:3000
```

ทุกครั้งที่แก้โค้ด หน้าเว็บจะรีเฟรชอัตโนมัติ

---

### 3. สร้าง Production Build

ถ้าต้องการลองรันแบบใกล้เคียงเซิร์ฟเวอร์จริง ให้รัน:

```bash
npm run build    # สร้างไฟล์ build
npm start        # รันเซิร์ฟเวอร์จาก build ที่สร้างไว้
```

โดยค่าเริ่มต้นเซิร์ฟเวอร์จะรันที่

```text
http://localhost:3000
```

---

### 4. Scripts อื่น ๆ

สามารถดู scripts เพิ่มเติมได้ที่ส่วน `scripts` ในไฟล์ `package.json` ของโปรเจกต์นี้
